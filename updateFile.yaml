platform: linux
image_resource:
  type: registry-image
  source:
    repository: alpine
inputs:
  - name: helm-chart-ingress
outputs:
  - name: helm-chart-ingress
run:
  path: /bin/sh
  args: 
    - -c
    - |
      ls -al
      date >> ./helm-chart/file
      echo "adding in taskfile" >> ./helm-chart-ingress/file
      echo "------------------" >> ./helm-chart-ingress/file
      echo "content of ./helm-chart/file"
      cat ./helm-chart-ingress/file
